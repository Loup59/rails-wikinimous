<div class="container">
  <div>
    <% @articles.each do |article| %>
    <li><%= link_to article.title, article %></li>
    <p><%= article.content %></p>
  <% end %>
  </div>
</div>

<%= link_to "add new article", new_article_path %>
